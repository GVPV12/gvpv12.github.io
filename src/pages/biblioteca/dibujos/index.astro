---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { Image } from 'astro:assets';

// Importamos las imágenes individualmente (Vite necesita esto para procesarlas)
import imgAligned from '@assets/img/aligned.webp';
import imgHibrida from '@assets/img/hibridachibi.webp';
import imgTodoroki from '@assets/img/todoroki.webp';
import imgGuilty from '@assets/img/guiltycrown.webp';
import imgLibrarian from '@assets/img/colored (1).webp';
import imgCyra from '@assets/img/cyra.jpg';
import imgEgo from '@assets/img/egochains.webp';
import imgImagination from '@assets/img/imagination.webp';

const dibujos = [
  { 
    src: imgAligned, 
    alt: 'Papel negro',
    slug: 'papelnegro'
  },
  { 
    src: imgHibrida, 
    alt: 'Linearts',
    slug: 'lineart'
  },
  { 
    src: imgTodoroki, 
    alt: 'Editados con efecto neón',
    slug: 'efectoneon'
  },
  { 
    src: imgGuilty, 
    alt: 'Activismo (trigger warning)',
    slug: 'activismo'
  },
  { 
    src: imgLibrarian, 
    alt: 'Coloreados',
    slug: 'coloreados'
  },
  { 
    src: imgCyra, 
    alt: 'Sketches',
    slug: 'sketches'
  },
  { 
    src: imgEgo, 
    alt: 'Vent art (trigger warning)',
    slug: 'ventart'
  },
  { 
    src: imgImagination, 
    alt: 'Pinturas (acuarela, acrilico, paisajes)',
    slug: 'pinturas'
  },
];
---

<BaseLayout
  title="Dibujos"
  description="Mi galería de arte digital"
>
  <div class="container">
    <main>
      <h1 class="text-3xl font-bold mb-2">Dibujos</h1>
      <p class="text-zinc-700 dark:text-zinc-300 text-xs mb-10">Galería de mis ilustraciones. Mis favoritas son los sketches, los linearts y las pinturas. Pero sobre todo mi arte activista y vent art (trigger warning). Muchos de mis dibujos los he simplemente hecho sin pensar en el resultado algo en concreto y han sido catarsis de sentimientos que he querido dejar salir o entender, por eso el nombre del dibujo o la explicación en algunos pudieron haber sido dados luego de analizar como me sentia en ese momento. Y varios dibujos que he hecho tienen multiples significados. Una de las razones por las que más amo dibujar es esa, más que hacer arte precioso o perfecto es la catarsis que logro con ellos. Algunos dibujos tienen la etiqueta @gv.art.e que es mi instagram viejo, el user de mi ig actualizado es @grevaleartdraws.</p>
      
      <div class="instagram-grid">
        {dibujos.map((dibujo) => (
          <a href={`/biblioteca/dibujos/${dibujo.slug}/`} class="grid-item" title={dibujo.alt}>
            {/* Usamos el componente Image de Astro */}
            <Image src={dibujo.src} alt={dibujo.alt} width={400} height={400} />
          </a>
        ))}
      </div>
    </main>
  </div>
</BaseLayout>

<style>
  /* Tu CSS se mantiene igual, pero asegúrate de que el selector apunte bien */
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  .instagram-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1rem;
  }

  .grid-item {
    position: relative;
    width: 100%;
    aspect-ratio: 1 / 1; /* Simplificado con aspect-ratio moderno */
    overflow: hidden;
    border-radius: 0.5rem;
    background-color: #f0f0f0;
    cursor: pointer;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  :global(.dark) .grid-item {
    background-color: #2a2a2a;
  }

  .grid-item:hover {
    transform: scale(1.02);
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
  }

  /* Importante: El componente Image genera un img, así que esto sigue funcionando */
  .grid-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  @media (max-width: 1024px) {
    .instagram-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  @media (max-width: 768px) {
    .instagram-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 0.75rem;
    }
  }

  @media (max-width: 480px) {
    .instagram-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 0.5rem;
    }
  }
</style>